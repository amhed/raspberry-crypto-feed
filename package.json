{
    "name": "crypto-ticker",
    "scripts": {
        "start": "echo 'Source will rebuild on changes' && nodemon --config nodemon.build.json",
        "sync": "echo 'Changes will be pushed to Raspberry Pi' && nodemon --config nodemon.sync.json"
    },
    "dependencies": {
        "bignumber.js": "^9.0.1",
        "chalk": "^4.1.1",
        "coingecko-api": "^1.0.10",
        "color": "^3.1.3",
        "lodash": "^4.17.21",
        "nodemon": "^2.0.7",
        "rpi-led-matrix": "^1.11.1",
        "rsync": "^0.6.1"
    }
}
